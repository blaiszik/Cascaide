# Session log

## 2026-06-02 13:22

Onboarded Cascaide to Continuity: scaffolded .continuity/ (index, handoff, workboard, decisions, research-directions) and registered it in continuity.config.json (restarted server to load it). Documented both pipelines: image 2D-UNet DDPM (cascaide/) and sp_cas.py set/point DiT + count head. Confirmed 2 bugs: (1) TanhP99 inference broken (infer.py:244 build_encoder lacks dataset=); (2) energy_vector conditioner ImportError (EnergyVectorConditioner vs EnergyEmbedConditioner). Next: persist normalization params in checkpoints to fix TanhP99 inference. research-directions.md covers benchmarks/normalization/architectures/autoresearch.

## 2026-06-02 13:35

Installed ovito via mamba env "cascaide" (python 3.11, ovito 3.15, numpy 2.4) from conda.ovito.org — see runbook.md. Verified ovito reads vac+sia dumps; Frenkel conservation HOLDS (0/25), correcting a false-violation from a hand-rolled parser (SIA dumps have extra cols c_myKE c_myPE). Real-data facts: cascades are SMALL (~18/35/78 defects at 20/50/100 keV; max ~112) — synthetic is ~10x oversized, so H*W/2 cap is NOT binding for this corpus. Flipped research-directions to real-subset-first (cache 64-256 real cascades to .npz, zero-dep after). Added runbook.md. Next: torch not yet installed in env (pip install -e . to train).

## 2026-06-02 13:59

Built model-progress tooling (June 2 entry in journal.md): cascaide/eval/ (io/metrics/scorecard/registry/generators), RadialDensityAuxLoss (the missing radial loss, wired+config), viewer/dashboard.html (HPC-results-aware human viewer), scripts/{build_real_subset,run_benchmark}.py, tests/test_eval.py (6 pass), docs/benchmarks.md. Scorecard provably isolates the captures-shape-misses-substructure failure (blob passes count/radial, fails g(r)/cluster/NN). HPC results store: results/runs/<id>/{run.json,scorecard.json} -> registry.collect() -> index.json -> dashboard. Fixed core.py CosineSchedule import bug. Pre-existing test bug remains (test_diffusion_core t-range vs T=50). Tests need PYTEST_DISABLE_PLUGIN_AUTOLOAD=1.

## 2026-06-02 14:46

Results now prominently viewable in Continuity: Cascaide human_entry=results/report.html (cover embeds it via iframe/mirror). Built report.py (self-contained, base64-inlined) + render.py (generated-vs-real cascade images per energy). Launched 300-epoch set-DiT training on 1200 real cascades (MPS); interim report at ~ep60 is live: 4 runs (3 baselines + real model). Set-DiT ~ep60 score 1.08 — radial_js 0.013 PASS (global shape good) but rdf/cluster/nn FAIL (substructure missed); visual confirms generated clouds are too compact/merged vs dispersed real cascades. Will run final benchmark when training hits 300 epochs.

## 2026-06-02 15:27

300-epoch set-DiT run done. RESULT: fully-trained sp_cas DiT is well-shaped (ep300 score 0.453, 6/7 pass; cluster_l1 0.049, nn_w1 0.60 both pass). Earlier "misses substructure" was UNDERTRAINING, not architecture — confirms researcher: sp_cas is current best, build on it. val-MSE selection is wrong: val-selected ep~50 scores 0.772 vs ep300 0.453 -> select by scorecard. Lone gap: rdf_l1 0.229 vs 0.20 (short-range pair corr), exactly what the substructure loss targets -> sharp hypothesis for Modal sweep. Modal launcher built (deploy/modal/, separate), NOT submitted. All runs viewable in Continuity cover (report). Items 1-4 + Modal built/tested.

## 2026-06-02 16:19

REAL Modal sweep LAUNCHED: 4 parallel T4 containers, full 3000 real cascades, 400 epochs, w_struct {0,0.3,0.6,1.0}. w_struct=0 = faithful sp_cas reproduction (control); question = does the substructure loss push rdf_l1 (the lone failing metric at ep300, 0.229 vs 0.20) under target without hurting the rest. Artifact collection FIXED + verified: each run dir is now self-contained (model.pt 28MB + scorecard + manifest + gen-vs-real images) and the results/runs/** downloader pulls weights back. Running ~12s/epoch -> ~1.5hr wall, est ~$3.5. On completion: auto-download + rebuild index/report -> dashboard/Continuity.

## 2026-06-02 17:54

Recipe changes shipped + EMA baseline submitted. (1) EMA added to train_set_v2 (on by default, decay 0.9999; used for select+sample+save). (2) scripts/rescore.py for fair 48/energy multi-seed scoring -> updates dashboard. (3) substructure loss demoted (default w_struct=0). (4) Min-SNR-gamma weighting WIRED but default OFF (ready, not confounding). Runs are now self-describing (label/config/tags include EMA/minSNR/w). Submitted 1 EMA baseline to Modal (w=0, EMA, 400ep, T4) = clean A/B vs the sweep no-EMA control. Plan when both land: rescore ALL run dirs at 48/energy x2 seeds for apples-to-apples vs the 0.453 baseline.

## 2026-06-02 18:05

Sweep landed. CONFIRMED: artifact fix works on real sweep (every run has model.pt 28MB) + Modal provenance tagged (scheduler=modal, job ids). BUG found+fixed: run_id collided (pre-EMA sweep used identical label "set-dit v2" for all 4 runs; 2 finished same UTC second -> same content-hash id -> w=0.3 overwritten; 3/4 dirs survived). Low science impact (loss-hurts trend already clear) but fixed registry.make_run_id to use a uuid4 suffix (robust under parallel same-second runs). Launched fair rescore of the 3 sweep runs at 48/energy (MPS). EMA baseline (w=0, EMA) still running on Modal. Next: fair numbers when rescore lands; full EMA-vs-control comparison when the EMA run lands.

## 2026-06-02 18:34

EMA BUG found+fixed. First EMA run gave scorecard 5.969 at ep100 because fixed decay=0.9999 with only ~9k steps left the shadow ~40% random-init (decay must match step count). Killed that Modal run (stopped app, no extra billing). Fix: EMAWarmup in train_set_v2 (effective decay = min(target,(1+step)/(10+step))) so shadow tracks model early. Smoke confirmed: warmup EMA = 1.18 at ep30 (vs 5.97 at ep100 broken). Also fixed run_id collision (uuid suffix). Resubmitted ONE longer EMA baseline (w=0, warmup EMA, 500ep, T4); monitoring first selection (ep100) to confirm not broken. Sweep rescore (3 runs @48/energy) still running on MPS.

## 2026-06-02 18:59

EMA fix CONFIRMED on Modal: warmup EMA run scores 0.492 at ep100 (vs 5.97 for the broken fixed-decay version; matches no-EMA control 12-sample 0.477). EMA now tracks the model -> not broken, no extra fix-loop needed. Letting the 500ep run finish (~80min), then will fair-rescore at 48/energy for the EMA-vs-control verdict. Reminder of fair sweep results: no-EMA control 0.472 (6/7); only failing metric now is vac_sia_sep (4.0 vs 3.0) since more data (3000) closed the g(r)/rdf gap (0.151, passes). Pair-relative encoding (built, unwired) is the data-justified next lever for vac-SIA.

## 2026-06-02 20:09

Wired pair-relative (Frenkel-pair) model for vac-SIA: cascaide/setdiff/paired.py (6-D PairedSetDenoiser DiT, PairNormalizer, dataset, sample_paired, save_paired) + setdiff/ema.py (shared warmup EMA) + generators.generate_paired_checkpoint + run_benchmark --generator paired-checkpoint + scripts/train_paired.py. Tested (22/22) + end-to-end smoke (self-contained run dir w/ model.pt). Caveat: no GT Frenkel labels -> Hungarian gives a min-cost matching; disp scale ~58A (vs vac ~140A), partial reduction, so vac-SIA improvement is empirical TBD. Added metrics: partial_rdf (vv/ss/vs), cross_nn_distances (Frenkel separation dist), radius_of_gyration, largest_cluster_frac, gyration_anisotropy. Captured top 3 as ADDITIVE scorecard diagnostics (vac_sia_nn_w1, rg_w1, largest_cluster_frac) - not weighted yet, so scores stay comparable. Verified discriminating (blob: vac_sia_nn_w1 2.8, lcf 0.37 vs real 0.23). EMA Modal run ~ep400, trending better than no-EMA control.

## 2026-06-02 20:14

Post-testing all models on the new metrics + surfacing in Continuity. report.py now renders a diagnostics table per run (vac->SIA NN dist W1, radius-of-gyration W1, largest-cluster fraction gen-vs-real). scripts/posttest.py re-evaluates every model on the full metric set and refreshes the report: drops stale diagnostic-less runs, re-runs baselines, re-benchmarks the local best/final checkpoints, re-scores the sweep model.pt runs in place (uniform n=32/energy for comparability). Running now (~22min MPS). The Continuity cover (results/report.html) will show the new per-model metrics on completion. EMA Modal run at ep420/500; will fold it in (re-run posttest) when it lands.

## 2026-06-02 20:52

DONE: post-tested all 8 models on the full metric set (incl. new diagnostics) and surfaced them on the Continuity cover (results/report.html, verified served via mirror). report.py renders a per-run diagnostics table (vac->SIA NN W1, Rg W1, largest-cluster frac). Final dashboard: copy 0.0 | jitter 0.434 | set-dit ep300 0.444 (7/7, best real model) | v2 control 0.518 | best-val 0.857 | v2 w=0.6 0.924 | v2 w=1.0 1.084 | blob 1.772. New vac_sia_nn_w1 confirms substructure loss worsens vac-SIA (3.5-4.0). EMA Modal run timed out (2h cap; fixed->5h) but EMA already verified working; user chose to STOP new runs and finalize. Paired model wired+tested (not trained at scale). scripts/posttest.py is idempotent to fold in future models.

## 2026-06-02 22:18

Added raw_data/ (gitignored, 254MB): 7100 minimized cascades across 9 CONTINUOUS energy ranges 0-300 keV (W, 1073K) — much richer than the old 3-point axis. Wrote scripts/process_raw_data.py (parallel pure-numpy parser, no ovito; robust to sia extra cols) -> data/cascaide_cascades.npz (17MB, loads via io.load_subset + extras: n_vac/n_sia/centroid/energy_range/supercell). 0 Frenkel violations, energy 0.01-299.8 keV, defects 0-1107. CRITICAL: supercell scales with energy (sc50->sc280) so absolute frames differ -> downstream MUST center per-cascade (global centroid is meaningless); centroid array stored. Big high-E cascades (max 1107 defects ~2200 tokens) -> O(N^2) attention heavy, needs train-time handling. .gitignore created (raw_data/, data/*.npz, results/, runs/, pycache, DS_Store). See runbook.md.

## 2026-06-02 22:34

Folded per-cascade centering into normalizers (cascaide/setdiff/data.py): PerCascadeNormalizer (center each cascade on its vac+SIA union centroid + global per-axis p99 scale; preserves vac-SIA relative geometry; serializes as G=0 CoordNormalizer so existing generate path works unchanged; isotropic s~86A) + SetDataset. Wired into train_set_v2 with --center/--energy_max/--energy_bin/--count_cap; scorecard.compute + generators._energy_counts now accept energy_bin for continuous energies. Tests 16/16. Smoke-verified full pipeline (train->select->save->finalize->results-store, per-cascade). LAUNCHED real run locally (MPS): <100keV subset of the new data (3290 cascades, continuous 0-100keV), 300 epochs, EMA, per-cascade centering, energy_bin=10 -> runs/setv2_lt100, writes to results/ dashboard for comparison vs old runs (ep300 0.444). ~2h. Ran local not Modal (avoid timeout/budget; comparable scale to old data).

## 2026-06-03 01:00

OVERNIGHT Modal run submitted (unattended): full 0-300 keV continuous data (7099 cascades after max_defects=1000 dropped 1), per-cascade centering, 250 epochs, EMA, energy_bin=25 (12 bins 0-300), A100. LIMIT: hard 3h Modal function timeout so it cannot run all night; sized to finish ~1.5h (ep1=20s/epoch, no OOM on big-cascade batches -> memory safe). On completion: auto-download + score + render + rebuild Continuity dashboard. Est cost ~$4-7 (A100 ~1.5h); budget ~$5.3 used. Risk: if it hits the 3h ceiling the run is lost (finalize wont download) but margin is large. This is the first training on the FULL continuous energy axis. modal_app reconfigured to a single bounded run + uploads cascaide_cascades.npz; added --max_defects safety cap to train_set_v2.

## 2026-06-03 01:06

Made the overnight run SLEEP-SAFE. Original modal run was NOT sleep-safe (ephemeral app tied to client + results downloaded locally -> sleep/disconnect would lose it). Stopped it (no orphan billing). Rebuilt modal_app: function writes results store to a persistent Modal Volume (cascaide-results) + commit(); launched with `modal run --detach ::main` so the cloud app survives laptop sleep/disconnect. Run healthy: full 0-300 keV (7099 cascades), per-cascade centering, 250ep, A100, ~22s/epoch (no OOM), hard 3h ceiling, ~<2h expected. Nothing depends on the laptop DURING the run. FETCH when back: `modal run deploy/modal/modal_app.py::fetch` -> pulls Volume to local results/ + rebuilds dashboard. Est cost ~$4-7.

## 2026-06-03 09:24

Built Polaris (ALCF) submit kit in deploy/polaris/: config.sh (edit-once: PROJECT/QUEUE/Eagle paths/Globus UUIDs), setup_env.sh (venv on Eagle over ALCF module load conda + pip install -e .), submit.sh (train=1 A100 / sweep=1 experiment per GPU across nodes, auto-sizes nodes), train.pbs, sweep.pbs+gpu_worker.sh (PALS rank->GPU), experiments.txt (design matrix), globus.sh (push npz->Eagle / pull results+ckpts->local), README.md (workflow + queue table). Workflow: code via git pull on Polaris, data/artifacts via Globus local<->Eagle, Polaris reads/writes /eagle directly. Added --no_report to train_set_v2 (parallel sweep ranks dont race on index.json; sweep rebuilds once). All bash -n clean + train_set_v2 compiles. NOT verifiable from here (module path/endpoint UUIDs/PROJECT need first-run confirmation on Polaris). NEXT: everything this session is uncommitted (eval/setdiff/deploy/docs/scripts/viewer/sp_cas + edits) -> must commit+push for the Polaris git pull.

## 2026-06-03 09:35

Pushed session work to FORK blaiszik/Cascaide branch tanhp99-global-centering (commit 8eabb005). origin (vigsam-coder/Cascaide) denies blaiszik write access -> use the fork; Polaris pulls from blaiszik/Cascaide. Polaris scripts updated to user conda pattern (module use /soft/modulefiles; module load conda; conda activate $ENV_PREFIX); setup_env clones ALCF base for CUDA torch. Git workflow going forward: commit to this branch + push to fork remote; git pull on Polaris. Remotes: origin=vigsam (upstream, RO), fork=blaiszik (push target).

## 2026-06-03 10:42

CONTEXT HANDOFF: wrote .continuity/polaris.md (resume steps + gotchas) + index pointer. Polaris kit pushed to fork blaiszik/Cascaide@tanhp99-global-centering (commit c235fa93). RESUME: on Polaris git pull + source config (DATA=/lus/eagle/projects/Cascaide/models/data/cascaide_cascades.npz), from Mac globus.sh push the npz, ls $DATA, then submit.sh train smoke test (debug-scaling, 30ep). Key gotchas captured: push to fork not origin (vigsam RO); Eagle=/lus/eagle/projects/...; config paths now direct-assign (stale-export bug fixed); venv-on-base NOT conda clone (Lustre 158k-file slowness); queue debug-scaling; max_defects/no_report; verify EAGLE_COLLECTION_ROOT + PROJECT + endpoint UUIDs on first run. Not yet run a successful Polaris job.

## 2026-06-03 16:40

FIRST SUCCESSFUL POLARIS RUN analyzed + next experiments built. Pulled job 7183018 (30ep, per_cascade, <100keV) via globus to local; scored best_by_scorecard.pt = 1.242, 4/7 (PASS count/conservation/radial/cluster; FAIL rdf_l1/nn_w1/vac_sia_sep) — the classic undertraining signature (global shape right, substructure wrong; resolved by full training before). Cluster finalize DOES work (results/runs on Eagle; my earlier "results empty" was a race vs the post-ep30 T=1000 finalize sampling — do not relitigate). Timing on Polaris A100 = ~2.5s/epoch on <100keV (3.3k cascades) -> 600ep ~25min, 1200ep ~50min, all fit debug-scaling 1h; bigger models cost more wall-clock (512/10 busts 1h). Scored vignesh_model.pt (GLOBAL-center, ep569, gitignored at repo root): <100keV = 1.489, 2/7 — WORSE than our 30ep per_cascade despite 19x training -> supports per_cascade > global on energy-scaling-supercell data (confounds: vignesh trained full 0-300keV + small n=12; clean per_cascade-vs-global cell would settle it). IMPLEMENTED + smoke-tested (all 4 schedulers + aug, exit 0): train_set_v2 --augment_rot (SO(3), per_cascade only; data.py SetDataset now stores physical centered coords, rotates pre-scale, no-aug byte-identical) and --lr_sched {cosine|cosine_warmup|onecycle|plateau} (onecycle steps/batch, cosine/warmup steps/epoch, plateau steps on the scorecard; +--warmup_frac/--lr_max/--plateau_*; LR logged/epoch; sched in run config+tags). deploy/polaris/experiments.txt = EXPERIMENT 1 LR-scheduler sweep (4 cells/1 node: cosine, cosine_warmup, onecycle@4xlr, plateau; all 256/6 <100keV 600ep finalize_n16) + EXP 2 (aug/arch/epochs) staged as comments. Committed + pushed to fork. GOTCHA: globus pull landed at ~/Desktop/runs not repo/runs ($REPO expanded one level shallow in globus.sh) -> verify dest after pull. NEXT: on Polaris git pull + submit.sh sweep (debug-scaling); then rescore the 4 winners at n~48 locally (posttest.py) for the verdict; Exp 2 with winning scheduler; consider clean global-vs-per_cascade centering run.

## 2026-06-03 17:10

Added a PREEMPTABLE-queue sweep + an experiment REGISTRY. deploy/polaris/experiments_preempt.txt = full 0-300keV study, 4 cells/1 node (per_cascade base 256/6 800ep · SAME but --center global [clean per_cascade-vs-global, resolves the vignesh confound] · 512/10 800ep converged [busts the 1h cap] · 256/6 1500ep ceiling), all cosine, --max_defects 1000 + batch 16 (8 for 512/10) for the big high-E clouds. Submit: QUEUE=preemptable WALLTIME=08:00:00 ./deploy/polaris/submit.sh sweep deploy/polaris/experiments_preempt.txt. These are DISTINCT from Exp 1 (different queue/range/axis; no shared cell). GOTCHA fixed: gpu_worker.sh runs `python ... $EXP` so experiment lines must have NO trailing #-comments (they'd become bad argv) — describe cells on their own comment lines above. Wrote .continuity/experiments.md (re-runnable defs of all sweeps + preemption recovery: best_by_scorecard.pt is saved to Eagle on every scorecard improvement so it survives a kill; no auto-resume -> recover by pulling the partial best ckpts and/or re-submitting the same file) + index.md pointer. Committed + pushed to fork.

## 2026-06-03 21:30

SCORING INFRA + INFERENCE-SPEED findings (big session). (1) Built A100 batch scoring: scripts/score_checkpoint.py (generate n/energy -> OVERALL + per-energy-REGIME scorecard low<50/mid50-150/high>150 keV, stored in run.json provenance.config.regime_scores) + deploy/polaris/{score.pbs,score_worker.sh} + `submit.sh score <checkpoints.txt>` (1 ckpt/GPU, nodes=ceil(N/4)). (2) Login-node CPU path deploy/polaris/score_login.sh (parallel, no queue) — but CPU is impractical: cost is the 1000 sequential sampling steps, NOT cloud size. (3) Staged 11 distinct local checkpoints to Eagle /Cascaide/models/rescore/ via a Globus BATCH push; checkpoints.txt + split checkpoints_lt100.txt (<100keV, login-OK) / checkpoints_full.txt (full-range, A100, n=12). (4) PER-CLOUD COST measured (MPS full-DDPM): 30keV/45tok 1.9s, 100keV/135tok 3.1s, 200keV/579tok 15s, 300keV/2022tok 120s — O(N^2) attn x T=1000; high-E tail dominates. A100 node (4 GPU) est ~1 cloud/s averaged over 0-300keV (low-E ~0.3-0.5s, high-E ~15-25s/cloud). (5) CLARIFIED how the report's stats were scored locally: baselines do no sampling; old runs/<100keV models have small clouds (~2.5-3s/cloud); the one full 0-300keV model (250ep 0.644) was scored on MODAL A100 (hpc.scheduler=modal), NOT local. (6) FAST SAMPLING: DDIM ruled out (needs ~500 steps to match -> no speedup; sampler-sensitive). DPM-Solver++(2M) with UNIFORM-LOG-SNR (lambda) spacing: ~30 steps within n=32 noise of full DDPM-1000 (dpmpp-30 0.789 / dpmpp-50 1.098 vs full 0.959 on converged 300ep), ~15-30x faster (0.1 vs 2.9 s/cloud), SAME model no retraining. Opt-in: score_checkpoint --sampler {ddpm,ddim,dpmpp} (default ddpm faithful); sp_cas.CoordDiffusion.sample_ddim/sample_dpmpp + generate(steps=,sampler=). Caveat: confirm at higher n/seed on A100 before trusting dpmpp for VERDICTS; low-E + full-range high-E regimes less validated. Bigger speedups need work: distillation/consistency or rectified-flow (retrain->1-8 steps), latent diffusion / local attention (cut per-step O(N^2)). (7) LATTICE-SNAP decode RULED OUT: real W defects sit ~1.0A (median ~a/3) off BCC sites at 1073K (thermal+strained cores) -> snapping worsens nn_w1/rdf. (8) Wrote scripts/sampling_scan.py -> .continuity/sampling-scan.md (Continuity report page: dpmpp-30 OVERALL+per-regime across ALL local models). All committed+pushed to fork (heads through f0238fb6).

## 2026-06-04 14:30

SCORECARD MINING + EVAL FIX (see `.continuity/scorecard-findings.md` for the full report). Pulled the 2026-06-04 A100 score batch from Eagle via Globus (15 per-regime scorecards; had to start Globus Connect Personal — it was disconnected). Built the cross-model per-regime + per-metric tables. Findings: (1) **High-E is a CONVERGENCE problem** — converged 250ep full-range per_cascade/EMA scores high-regime 0.473 vs 2.3–3.6 for un-converged set-dit-v2 baselines (best overall 0.656); validates Wave-1 A, sets the bar for cell D (must beat 0.473). (2) **The low-regime "undercount" was a SCORECARD ARTIFACT, not the count head.** The scorer conditioned generation at the BIN CENTER; bin-0=[0,12.5)keV has its center at the left edge (0 keV ≈ no cascade) vs a reference window averaging 5.55 keV. Head is accurate: predicts 7.44 pairs at E=5.55 (real 7.67) but 3.16 at E=0 (what the scorer used). "Worsens with training" = head getting MORE correct about the E→0 limit, which the artifact penalizes. Contaminated bin-0 nn/cluster/vss too → the whole LOW regime, for every model. FIX: `score_checkpoint.py --gen_energy {sample|bin_mean|center}` (default now **sample** = condition each gen cloud on a real reference energy; `center`=legacy, kept for reproducing pre-today scores). `sp_cas.generate` now takes a per-sample energy vector (free); `generators._energy_targets` builds the per-bin conditioning energies; mode recorded in run.json config; +test_energy_targets_modes (eval tests 7/7, suite 25/25). A/B on 250ep (dpmpp-20, only gen_energy varies): LOW score 1.480→**0.693**, bin-0 count_mape 0.625→**0.032** (gen 5.8→15.8 vs real 15.3), nn_w1 5.42→1.36, cluster_l1 0.413→0.111. (3) **The ONE genuine model weakness is pairwise substructure (RDF)** — `rdf_l1≈0.13–0.41` while radial/count/cluster/nn/conservation are all near-ceiling. → next experiments target RDF via the `pairwise_hist_loss` (w_struct). (4) **Radial density is SOLVED** (rad_js≈0.004) — the number behind "radial aux loss hurt" (zero headroom → pure gradient conflict). (5) **per_cascade > global centering ~3× on RDF** (vignesh global-center ep569: rdf_l1 0.407 vs per_cascade 0.131; converged, so it's normalization not undertraining; global wins on absolute vac-sia sep though; n=1 global → clean A/B warranted). (6) Spatial fidelity is convergence (baselines over-separate Frenkel pairs gen≈22 vs real≈15). IMPLICATIONS: Wave-1 cell B (--energy_balance) de-motivated (high-E starvation disproven, bin-0 not rare); re-score backlog with --gen_energy sample before trusting low regime; per_cascade-vs-global A/B now well-motivated.

## 2026-06-04 18:30

SUBSTRUCTURE (RDF) AUX LOSS = CLEAN NEGATIVE (full team-briefing write-up: `.continuity/substructure-loss-negative.md`). Built a metric-matched differentiable g(r) loss to attack rdf_l1 (thought to be the model's weakness). Fixed TWO methodology problems first: (a) range mismatch — the legacy pairwise_hist_loss used fixed rmax=6A but nn dist is 7-11A and mean pairwise dist 30-96A, so 6A held only 1-8% of pairs (this is why the old "--w_struct hurts at w>=0.3" sweep failed); replaced with rdf_hist_loss = per-cloud adaptive rmax (90th-pct) + ideal-shell norm (--struct_mode rdf). (b) per-cloud noise — first version did per-cloud-L1-then-average; metric does average-then-L1, so rewrote to batch-average g(r) BEFORE the relative L1 (matches scorecard._per_energy). CLEAN TEST job 7185401 (4 cells, ALL ep500, scored --gen_energy sample dpmpp-20 n=48): control w_struct=0 OVERALL ~0.4-0.55 PASSES 7/7 (rdf_l1 0.16<0.20 target); loss cells monotonically WORSE on every metric incl rdf_l1 itself (w0.3/1.0/3.0 -> OVERALL 1.45/1.99/2.26, rdf_l1 0.52/0.76/0.75). REFRAME: the converged per_cascade <100keV model already passes the WHOLE scorecard — rdf_l1 was the largest-MAGNITUDE metric but within target, not a failing requirement. So we optimized a non-problem and broke a passing model. GENERAL LESSON (2nd coordinate aux loss to fail, after radial): don't bolt distributional losses on this model's coordinate objective — they're global/non-local and corrupt the score field, esp. gated at low-t (= the final sampling steps); the pure eps objective already produces passing substructure. STRATEGY: drop the substructure-loss line; <100keV is solved; real headroom is full-range high-E (convergence/capacity = Wave-1 A/D, still running). Loss code kept (correct+tested) but demoted to experimental (default off). First attempt job 7185324 died to a node RPC timeout (HSN infra failure, not walltime/code) at ep200/400 — its partials trended the same but were epoch-confounded; the local MPS equal-epoch A/B was killed (23s/epoch too slow) and rerun on Polaris instead. Commits ab734706 (eval --gen_energy fix), 1a3f9377 (per-cloud rdf loss), 7a6075db (batch-averaged rdf loss) pushed to fork. Docs: substructure-loss-negative.md (new, team-facing) + corrections to scorecard-findings.md / experiments.md (Exp 3 marked DONE-NEGATIVE) / index.md.

## 2026-06-05 14:20

CENTERING A/B (per_cascade vs global) + DATASET FIGURES + best-bet 500ep run launched. (1) CENTERING VERDICT: ran a global-centering full-range 250ep model on Modal (app cascaide-setv2), pulled both it and the matched per_cascade Modal run (same harness, EMA/cosine/250ep/full-range, only --center differs), rescored both identically (full-range, n=24, dpmpp-20, --gen_energy sample). per_cascade WINS every metric & regime: OVERALL 0.661 vs 0.882 (global +0.221), rdf_l1 0.168 vs 0.272 (+0.104/~62%), low +0.180, mid +0.267, high +0.191. Stronger than it looks: per_cascade was ep124 (its best) vs global ep249 — per_cascade wins despite half the training. Confirms the n=1 vignesh result with a clean A/B; re-calibrates magnitude (vignesh implied 3× on rdf; real penalty ~33% overall/~62% rdf). Mechanism: supercell frames differ per cascade → global centroid+dataset-wide scale (s≈270 vs per_cascade≈145) squashes per-cloud structure. eps-loss aside: global had LOWER training loss (~0.087 vs per_cascade ~0.18) — a normalization-scale artifact (bigger scale→smaller normalized coords→easier eps target), NOT better; predicted+confirmed global trails. => per_cascade stays default; global-centering branch premise fails. (2) DATASET COVERAGE FIGURES (for the team): scripts/plot_dataset_coverage.py → figures/{dataset_coverage.png/.pdf, dataset_sampling.png, dataset_summary.md}. 7,100 cascades, W 1073K, PKA 0.01-300 keV (9 MD campaigns, supercell sc50→sc280), Frenkel pairs 0-1107 (mean 98, N_vac==N_SIA, 0 violations), defect production N∝E^0.90 (log-log r=0.95); y-axis truncated at 99th pct (max 1107 outlier compresses bulk). Sampling densest 100-200 keV (1500/band), sparsest 200-300 keV (400/band). (3) BEST-BET MODEL LAUNCHED on Modal (app ap-KUIg1l1P6BaEjLTsvipaZR, detached): per_cascade 500ep + --augment_rot (SO(3); exact symmetry, targets data-starved high-E), 256/6 full-range, scoring DISABLED (--select_every 99999 --no_results), --save_every 25 (convergence trace ckpt_0025..0500.pt on the Volume under ckpts/percascade_500ep_aug/). ~3.3h ≈ $12. Picked pure convergence + augmentation because: high-E is convergence-limited (the only headroom), aug is the highest-upside low-risk lever left, and aux losses/global/energy_balance are all ruled out. (4) NEW CODE (committed+pushed, heads e9a7c29b→aa288b8d): train_set_v2 --save_every (distinct kept ckpts); modal_app exposes --augment_rot/--save_every/--no_results/--run_name, writes ckpts onto the Volume, PERIODIC VOL.commit() every ~2min (timeout/crash-safe — Modal doesn't auto-commit on SIGKILL), 5h timeout. score_checkpoint --gen_energy default flipped to sample earlier (ab734706). NEXT AGENT: when the Modal run lands, fetch ckpts/percascade_500ep_aug, rescore the trace offline (--gen_energy sample) for the score-vs-epoch convergence curve, and rescore final_model.pt vs the per_cascade-250ep baseline (results/runs/20260603T082541-set-dit-v2-87ec2c1b/model.pt) for the verdict. See handoff.md.

## 2026-06-05 17:35

BEST-BET RUN LANDED = NEW BEST MODEL (full writeup: `.continuity/convergence-500ep-augment.md`). The 500ep + --augment_rot Modal run (app ap-KUIg1l1P6BaEjLTsvipaZR) finished clean at ep 500/500; pulled all 20 ckpts + final_model.pt from the Volume (ckpts/percascade_500ep_aug/). (1) CONVERGENCE TRACE (answers "do they converge early?"): built scripts/score_trace.py (cached per-ckpt trace scorer that can TRAIL a live run — it scored checkpoints as they committed while training continued on the cloud A100, free local MPS) and scored all 20 ckpts at n=12 full-range dpmpp-20 --gen_energy sample. NO early plateau: OVERALL drifts ~0.85 (early) → ~0.70 (late) under heavy n=12 noise (~±0.1/pt), best band ep375–500 (mostly 6/7); ep500 holds run-minima on rdf_l1 0.167 / rdf_hi 0.140 / high-E 0.655 — longer training kept buying high-E + substructure quality (the only headroom). Figure figures/convergence_500ep_aug.{png,pdf,json}. (2) VERDICT (n=24, all 4 models in ONE process for matched comparison, scripts/score_verdict.py): baseline per_cascade 250ep (ep124) OVERALL 0.810 5/7; 500ep+aug ep375/475/500 = 0.613/0.615/0.623 ALL 6/7, each beating baseline on EVERY regime+rdf by ~0.19 OVERALL and −0.33 high-E (0.64 vs 0.967). Candidates tied within noise → KEEP final_model.pt (ep500) as new standing best per_cascade model; supersedes the 250ep baseline everywhere. (3) CAVEAT (important): baseline scored 0.810 here vs 0.661 in the 14:20 centering A/B for the IDENTICAL ckpt+settings — generators.generate_set_checkpoint(seed) only pins the _energy_targets draw, NOT the reverse-diffusion sampler, so absolute OVERALL wobbles ~±0.15 run-to-run even at n=24. Trust within-run/same-process deltas (this verdict is), not cross-run absolutes; the n=12 trace independently reproduces the ranking. Follow-up: thread seed → torch.manual_seed in sp.generate for reproducible absolutes. (4) COST correction: full-range MPS rescore is HIGHLY SUBLINEAR in n (n=2=194s, n=12=220s, n=24~5min) — the high-E batched tail dominates a ~fixed cost; the earlier "~4min/ckpt n=24 trace" note undercounted but a dense trace is still cheap (20-ckpt n=12 trace ~75min, ran concurrent with training). (5) DOCS: new convergence-500ep-augment.md (team briefing — both result sets + caveat + repro), index.md pointer, handoff.md IN-FLIGHT→LANDED + recipe now includes 500ep·--augment_rot as PROVEN positive levers. Tooling (scripts/score_trace.py, scripts/score_verdict.py) not yet committed — commit+push to fork next.

## 2026-06-06 09:00

REPORT CARDS + NEXT-WAVE PLAN written; pausing until the data drop. (1) Surfaced the 4 verdict models as cards in the Model Progress Report (results/report.html): scored final_model (ep500), ckpt_0375, ckpt_0475, and a matched rescore of the 250ep baseline via scripts/score_checkpoint.py (n=24 full-range dpmpp-20 --gen_energy sample) → each a ranked card with scorecard + per-energy curve + gen-vs-real renders. Matched-rescore scores (a THIRD independent draw): baseline 0.710 5/7, ep375 0.619, ep475 0.593, ep500 0.578 7/7 — reproduces the ranking (all 500ep+aug beat baseline, high-E 0.58–0.65 vs 0.80); baseline's three draws 0.661/0.710/0.810 are a live demo of the ±0.15 sampler-variance caveat. NOTE: --gen_energy sample makes render_comparison emit one image per CONTINUOUS energy (194/run) → report ballooned to 242MB; thinned each card to ~13 representative energies → 110MB. §8 added to convergence-500ep-augment.md. (2) NEXT-WAVE PLAN written into experiments.md as Wave 2 (Exp 6–9), driven by Monday 2026-06-08's ~1000 new HIGH-E cascades (hits the sparsest+weakest regime) + a long Polaris run. Exp 6 = Phase 0 eval hardening (PREREQUISITE: seed the sampler in sp.generate; add held-out --test_frac stratified split; multi-seed/higher-n high-E scoring; re-baseline final_model on held-out) — because every verdict this session was muddied by ±0.15 sampler noise + scoring on training data. Exp 7 = clean data A/B (old vs old+1000-high-E, same recipe, held-out high-E test) → "is high-E a DATA problem?". Exp 8 = the long run, 2×2 epochs{2000,4000}×capacity{256/6,512/10} on old+new, --save_every 100 + offline held-out convergence curves to find the plateau AND catch overfit (don't train blind; sparse high-E + big model = memorization risk). Exp 9 (conditional) = high-E specialist/curriculum (+--energy_min), O(3) reflection aug, --min_snr. Ruled-out (don't retry): aux losses, global centering, lattice-snap. Stretch: local/latent attention to cut O(N²) if epochs help but wall-clock-bound. Marked Exp 5 DONE + added the new best to the run ledger; handoff "NEXT" now points at Wave 2. RESTING until the data arrives Monday.
